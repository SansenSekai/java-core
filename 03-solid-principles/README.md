# Модуль 03: Принципы SOLID

> **Уровень**: ⭐⭐⭐ Средний+  
> **Время**: 4-5 часов  
> **Пререквизиты**: Модули 01-02

## 📋 Теоретическое введение

**SOLID** — это пять принципов объектно-ориентированного проектирования, которые помогают создавать гибкий и поддерживаемый код.

### S — Single Responsibility Principle (SRP)
> Класс должен иметь только одну причину для изменения.

**Плохо**: класс `UserService` регистрирует пользователей, отправляет email, сохраняет в БД, логирует действия.

**Хорошо**: `UserRegistrationService`, `EmailSender`, `UserRepository`, `AuditLogger` — каждый делает одно дело.

### O — Open/Closed Principle (OCP)
> Классы должны быть открыты для расширения, но закрыты для модификации.

**Плохо**: добавление нового типа требует изменения switch/if-else.

**Хорошо**: добавление нового типа = создание нового класса, реализующего интерфейс.

### L — Liskov Substitution Principle (LSP)
> Объекты подклассов должны быть заменяемы объектами базового класса без нарушения корректности программы.

**Плохо**: `Square extends Rectangle` с переопределением setWidth/setHeight, ломающим поведение.

**Хорошо**: `Square` и `Rectangle` реализуют общий интерфейс `Shape`, но не наследуют друг друга.

### I — Interface Segregation Principle (ISP)
> Много специфических интерфейсов лучше, чем один универсальный.

**Плохо**: интерфейс `Animal` с методами fly(), swim(), run() — не все животные умеют всё.

**Хорошо**: интерфейсы `Flyable`, `Swimmable`, `Runnable` — реализуй только нужное.

### D — Dependency Inversion Principle (DIP)
> Модули верхнего уровня не должны зависеть от модулей нижнего уровня. Оба должны зависеть от абстракций.

**Плохо**: `OrderService` создаёт внутри себя `MySqlDatabase` напрямую.

**Хорошо**: `OrderService` зависит от интерфейса `OrderRepository`, который может быть `MySqlOrderRepository`, `MongoOrderRepository` и т.д.

## 🎯 Задание

### Задача 1: Рефакторинг нарушений SRP

В пакете `srp.problem` находится класс `UserManager`, который делает слишком много:
- Валидирует пользователей
- Сохраняет в «базу данных»
- Отправляет уведомления
- Генерирует отчёты

**Требование**: Разбей `UserManager` на несколько классов с единственной ответственностью в пакете `srp.solution`.

### Задача 2: Применение OCP

В пакете `ocp.problem` находится `DiscountCalculator` с switch по типу скидки.

**Требование**: Рефакторь код так, чтобы добавление новой скидки не требовало изменения существующего кода.

### Задача 3: Исправление нарушения LSP

В пакете `lsp.problem` находится классическая проблема `Rectangle`/`Square`.

**Требование**: Перепроектируй иерархию так, чтобы принцип LSP не нарушался.

### Задача 4: Применение ISP

В пакете `isp.problem` находится «жирный» интерфейс `Worker` с методами `work()`, `eat()`, `sleep()`, `attendMeeting()`.

**Требование**: Раздели интерфейс на несколько маленьких, чтобы роботы не были обязаны реализовывать `eat()` и `sleep()`.

### Задача 5: Применение DIP

В пакете `dip.problem` находится `NotificationService`, который напрямую создаёт `SmtpEmailClient`.

**Требование**: Инвертируй зависимости, чтобы можно было легко заменить способ отправки уведомлений.

## 🧪 Тесты

```bash
mvn test
```

Тесты проверяют:
- Правильность рефакторинга (функциональность сохранена)
- Соблюдение принципов SOLID
- Возможность расширения без модификации

## 🤔 Мета-вопросы для размышления

1. Как SOLID связан с принципом «loose coupling, high cohesion»?
2. Может ли чрезмерное следование SOLID привести к over-engineering?
3. Как SOLID применяется в самом JDK? (примеры: `java.util.Collection`, `java.io.*`)
4. Что важнее: SRP или минимизация количества классов?
5. Как DIP реализуется без фреймворков (Spring)?

## ✅ Чек-лист самопроверки

### SRP
- [ ] Каждый класс имеет единственную ответственность
- [ ] Название класса точно отражает его назначение
- [ ] При изменении требований изменяется только один класс

### OCP
- [ ] Новый функционал добавляется через создание новых классов
- [ ] Существующий код не изменяется при расширении
- [ ] Используются абстракции (интерфейсы/абстрактные классы)

### LSP
- [ ] Подклассы не ослабляют постусловия
- [ ] Подклассы не усиливают предусловия
- [ ] Подклассы не бросают новые исключения
- [ ] Код работает корректно с любым подтипом

### ISP
- [ ] Интерфейсы маленькие и специфичные
- [ ] Классы не реализуют методы, которые им не нужны
- [ ] Нет «пустых» реализаций методов

### DIP
- [ ] Зависимости внедряются, а не создаются внутри класса
- [ ] Высокоуровневые модули зависят от абстракций
- [ ] Легко заменить реализацию без изменения клиентского кода

