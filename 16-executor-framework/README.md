# Модуль 16: Executor Framework

> **Уровень**: ⭐⭐⭐ Средний+  
> **Время**: 4-5 часов

## 📋 Теоретическое введение

### Типы Executors

```java
Executors.newFixedThreadPool(n)      // фиксированный пул
Executors.newCachedThreadPool()      // растущий пул
Executors.newSingleThreadExecutor()  // один поток
Executors.newScheduledThreadPool(n)  // отложенные задачи
```

### ThreadPoolExecutor

```java
new ThreadPoolExecutor(
    corePoolSize,      // минимум потоков
    maxPoolSize,       // максимум потоков
    keepAliveTime,     // время жизни idle потоков
    timeUnit,
    workQueue,         // очередь задач
    threadFactory,     // создание потоков
    rejectedHandler    // политика отклонения
);
```

### Политики отклонения

- **AbortPolicy**: бросает RejectedExecutionException
- **CallerRunsPolicy**: задача выполняется в вызывающем потоке
- **DiscardPolicy**: задача молча отбрасывается
- **DiscardOldestPolicy**: отбрасывается самая старая задача

## 🎯 Задание

### Задача 1: Правильное завершение

Реализуй graceful shutdown с обработкой незавершённых задач.

### Задача 2: Мониторинг пула

Отслеживай: активные потоки, задачи в очереди, выполненные задачи.

### Задача 3: Custom ThreadFactory

Создай ThreadFactory с:
- Именованием потоков
- UncaughtExceptionHandler
- Daemon/non-daemon

## ✅ Чек-лист

- [ ] Знаешь разницу между типами Executors
- [ ] Понимаешь параметры ThreadPoolExecutor
- [ ] Умеешь правильно завершать ExecutorService
- [ ] Знаешь про проблемы с Executors.newCachedThreadPool()

