# Модуль 13: Основы многопоточности

> **Уровень**: ⭐⭐⭐ Средний+  
> **Время**: 4-5 часов

## 📋 Теоретическое введение

### Создание потоков

1. **Наследование Thread**: `class MyThread extends Thread`
2. **Реализация Runnable**: `new Thread(runnable)`
3. **Callable + Future**: возврат результата и исключений

### Жизненный цикл потока

```
NEW → RUNNABLE → (BLOCKED/WAITING/TIMED_WAITING) → TERMINATED
```

### Прерывания

- `thread.interrupt()` — установить флаг прерывания
- `Thread.interrupted()` — проверить и сбросить флаг
- `thread.isInterrupted()` — проверить без сброса
- `InterruptedException` — бросается при ожидании

## 🎯 Задание

### Задача 1: Параллельный загрузчик файлов

Реализуй многопоточный загрузчик:
- N потоков загружают файлы параллельно
- Ограничение на количество одновременных загрузок
- Отмена загрузки через interrupt

### Задача 2: Producer-Consumer

Реализуй классический паттерн без использования BlockingQueue:
- Producer добавляет элементы в очередь
- Consumer извлекает элементы
- Ограниченный буфер

### Задача 3: Thread-safe счётчик

Реализуй счётчик без synchronized (используй AtomicInteger):
- increment(), decrement(), get()
- compareAndIncrement(expected)

## ✅ Чек-лист

- [ ] Понимаешь разницу Thread vs Runnable
- [ ] Умеешь правильно обрабатывать InterruptedException
- [ ] Знаешь про daemon threads
- [ ] Понимаешь thread safety

