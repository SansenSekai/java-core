# Модуль 02: Наследование и Полиморфизм

> **Уровень**: ⭐⭐ Средний  
> **Время**: 2-3 часа  
> **Пререквизиты**: Модуль 01 (Инкапсуляция)

## 📋 Теоретическое введение

### Наследование: IS-A vs HAS-A

Наследование — это механизм **повторного использования кода**, но он часто используется неправильно. Ключевой вопрос: **«Является ли A подвидом B?»** (IS-A)

- ✅ `Dog extends Animal` — собака ЯВЛЯЕТСЯ животным
- ❌ `Stack extends ArrayList` — стек НЕ ЯВЛЯЕТСЯ списком (это ошибка дизайна в JDK!)

**Принцип подстановки Лисков (LSP)**: если `S` — подтип `T`, то объекты типа `T` можно заменить объектами типа `S` без нарушения корректности программы.

### Полиморфизм

**Полиморфизм** позволяет работать с объектами разных типов через единый интерфейс:
- **Compile-time (перегрузка)**: выбор метода по сигнатуре
- **Runtime (переопределение)**: выбор метода по реальному типу объекта

### Абстрактные классы vs Интерфейсы

| Критерий | Абстрактный класс | Интерфейс |
|----------|-------------------|-----------|
| Наследование | Одиночное | Множественное |
| Состояние | Может иметь поля | Только константы |
| Конструктор | Есть | Нет |
| Методы | Любые | public (+ default, static) |
| Использование | Общая реализация | Контракт поведения |

### Типичные ошибки Senior-разработчиков

1. **Нарушение LSP**: подкласс бросает исключения, которые родитель не бросал
2. **Fragile Base Class**: изменение родителя ломает наследников
3. **Наследование вместо композиции**: создание глубоких иерархий
4. **Неправильный equals() в иерархии**: сравнение родителя с потомком

### Pattern Matching (Java 16+) — Современная альтернатива instanceof

**Старый способ:**
```java
if (shape instanceof Circle) {
    Circle c = (Circle) shape;
    return Math.PI * c.getRadius() * c.getRadius();
}
```

**Pattern Matching:**
```java
if (shape instanceof Circle c) {
    return Math.PI * c.getRadius() * c.getRadius();
}
```

**Pattern Matching в switch (Java 21):**
```java
return switch (shape) {
    case Circle c    -> Math.PI * c.radius() * c.radius();
    case Rectangle r -> r.width() * r.height();
    case Triangle t  -> 0.5 * t.base() * t.height();
    // Если sealed — не нужен default!
};
```

**⚠️ Когда использовать instanceof (даже с pattern matching)?**
- Операции над объектами из внешних библиотек
- Полиморфизм невозможен (нет контроля над иерархией)
- Visitor pattern не подходит

**✅ Когда НЕ использовать instanceof:**
- Вы контролируете иерархию — используйте полиморфизм!
- Много `instanceof` подряд — признак нарушения OCP

## 🎯 Задание

### Задача 1: Shape Hierarchy — классический полиморфизм

Создай иерархию геометрических фигур:
- Абстрактный базовый класс `Shape`
- Конкретные реализации: `Circle`, `Rectangle`, `Triangle`
- Каждая фигура умеет вычислять площадь и периметр

**Требования**:
1. `Shape` — абстрактный класс с абстрактными методами `area()` и `perimeter()`
2. Каждый конкретный класс валидирует свои параметры (радиус > 0, стороны > 0)
3. Реализуй `Comparable<Shape>` для сравнения по площади
4. Реализуй `equals()` и `hashCode()` — две фигуры равны, если они одного типа с одинаковыми параметрами

### Задача 2: Payment Processing — интерфейсы и стратегии

Реализуй систему обработки платежей:
- Интерфейс `PaymentProcessor` с методом `process(Payment payment)`
- Реализации: `CreditCardProcessor`, `PayPalProcessor`, `CryptoProcessor`
- Интерфейс `PaymentValidator` для валидации платежей

**Требования**:
1. Каждый процессор имеет свою логику валидации
2. `CreditCardProcessor` проверяет номер карты (алгоритм Луна)
3. `PayPalProcessor` проверяет формат email
4. `CryptoProcessor` проверяет формат адреса кошелька
5. Обработка возвращает `PaymentResult` с успехом/ошибкой

### Задача 3: Notification System — default методы в интерфейсах

Создай систему уведомлений с использованием default-методов:
- Интерфейс `Notifier` с методом `send(String message, String recipient)`
- Default-метод для логирования отправки
- Реализации: `EmailNotifier`, `SmsNotifier`, `PushNotifier`

**Требования**:
1. Все реализации логируют отправку через default-метод
2. `EmailNotifier` может отправлять HTML и plain text
3. `SmsNotifier` обрезает сообщение до 160 символов
4. Реализуй `CompositeNotifier`, который отправляет через несколько каналов

## 🧪 Тесты

```bash
mvn test
```

Тесты проверяют:
- Корректность вычислений (площадь, периметр)
- Полиморфное поведение (работа с коллекцией Shape)
- LSP: подклассы не нарушают контракт родителя
- Правильность equals/hashCode в иерархии

## 🤔 Мета-вопросы для размышления

1. Почему `Stack extends Vector` в JDK — это плохой дизайн?
2. В каких случаях стоит предпочесть абстрактный класс интерфейсу?
3. Что произойдёт, если переопределить метод с `throws IOException` методом без исключений?
4. Почему в Java 8 добавили default-методы в интерфейсы?
5. Как избежать проблемы «ромбовидного наследования» при множественном наследовании интерфейсов?

## ✅ Чек-лист самопроверки

### Наследование
- [ ] Иерархия следует принципу IS-A
- [ ] Подклассы не нарушают контракт родителя (LSP)
- [ ] Нет неоправданно глубоких иерархий (max 2-3 уровня)
- [ ] Рассмотрена альтернатива через композицию

### Полиморфизм
- [ ] Код работает с абстракциями, не с конкретными типами
- [ ] Можно добавить новую реализацию без изменения существующего кода
- [ ] Нет `instanceof` или switch по типу (code smell)

### equals/hashCode
- [ ] equals() симметричен в иерархии
- [ ] Объекты разных подклассов не равны (если это требуется)
- [ ] hashCode() согласован с equals()

### Тесты
- [ ] Все тесты проходят
- [ ] Есть тесты на полиморфное поведение
- [ ] Есть негативные тесты (некорректные параметры)

