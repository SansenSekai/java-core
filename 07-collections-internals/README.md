# Модуль 07: Внутреннее устройство коллекций

> **Уровень**: ⭐⭐⭐ Средний+  
> **Время**: 3-4 часа

## 📋 Теоретическое введение

### ArrayList

- Основан на **динамическом массиве**
- Начальная ёмкость: 10 (или 0, если создан через `new ArrayList<>()`)
- При переполнении растёт в **1.5 раза** (`newCapacity = oldCapacity + (oldCapacity >> 1)`)
- O(1) доступ по индексу, O(n) вставка в середину

### LinkedList

- **Двусвязный список** узлов
- Каждый узел хранит ссылки на prev и next
- O(n) доступ по индексу, O(1) вставка (если есть ссылка на узел)
- Реализует и `List`, и `Deque`

### Когда что использовать?

| Операция | ArrayList | LinkedList |
|----------|-----------|------------|
| get(i) | O(1) ✅ | O(n) |
| add(element) | O(1)* | O(1) ✅ |
| add(i, element) | O(n) | O(n) |
| remove(i) | O(n) | O(n) |
| Итерация | Быстрее (cache-friendly) | Медленнее |

*амортизированная сложность

## 🎯 Задание

### Задача 1: Реализуй SimpleArrayList

Создай упрощённую версию ArrayList с нуля:
- Динамическое расширение массива
- Методы: add, get, set, remove, size, isEmpty
- Правильная обработка null элементов

### Задача 2: Реализуй SimpleLinkedList  

Создай упрощённую версию LinkedList:
- Двусвязный список
- Методы: addFirst, addLast, removeFirst, removeLast, get
- Итератор

### Задача 3: Сравнительный анализ

Напиши бенчмарк (можно простой, с System.nanoTime()):
- Вставка 100,000 элементов в начало
- Вставка 100,000 элементов в конец
- Случайный доступ к 10,000 элементам
- Итерация по всем элементам

## ✅ Чек-лист

- [ ] SimpleArrayList корректно расширяется
- [ ] SimpleLinkedList поддерживает двусвязность
- [ ] Понимаешь, почему ArrayList обычно быстрее LinkedList
- [ ] Знаешь, когда LinkedList всё-таки лучше

