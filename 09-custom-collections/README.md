# Модуль 09: Собственные коллекции

> **Уровень**: ⭐⭐⭐⭐ Сложный  
> **Время**: 5-7 часов

## 📋 Теоретическое введение

### LRU Cache

**Least Recently Used** — при переполнении удаляется самый старый (редко используемый) элемент.

Реализация: LinkedHashMap с `accessOrder=true` или собственный doubly-linked list + HashMap.

### Ring Buffer (Circular Buffer)

Фиксированный массив с указателями head и tail. При переполнении перезаписывает старые элементы.

Применение: логирование, потоковая обработка, producer-consumer.

### Trie (Prefix Tree)

Дерево для хранения строк с общими префиксами. Эффективный поиск по префиксу.

## 🎯 Задание

### Задача 1: LRU Cache

Реализуй LRU Cache с O(1) для get и put:
- Фиксированный размер
- При переполнении удаляется самый старый
- При доступе элемент становится "свежим"

### Задача 2: Ring Buffer

Реализуй thread-safe ring buffer:
- offer(element) — добавить, вернуть false если полон
- poll() — извлечь, вернуть null если пуст
- forceOffer(element) — перезаписать старый если полон

### Задача 3: Trie

Реализуй Trie для строк:
- insert(word)
- search(word) — точное совпадение
- startsWith(prefix) — поиск по префиксу
- autocomplete(prefix, limit) — топ-N слов с префиксом

## ✅ Чек-лист

- [ ] LRU Cache O(1) для get и put
- [ ] Ring Buffer корректно работает с wrap-around
- [ ] Trie эффективен по памяти
- [ ] Есть юнит-тесты для edge cases

