# Модуль 21: Reflection API

> **Уровень**: ⭐⭐⭐ Средний+  
> **Время**: 4-5 часов

## 📋 Теоретическое введение

### Получение Class объекта

```java
Class<?> clazz = obj.getClass();           // от объекта
Class<?> clazz = String.class;             // литерал
Class<?> clazz = Class.forName("...");     // по имени
```

### Инспекция

```java
clazz.getDeclaredFields()        // все поля (включая private)
clazz.getFields()                // только public (включая унаследованные)
clazz.getDeclaredMethods()       // все методы класса
clazz.getConstructors()          // конструкторы
```

### Вызов методов

```java
Method method = clazz.getDeclaredMethod("name", String.class);
method.setAccessible(true);      // доступ к private
Object result = method.invoke(instance, "arg");
```

## 🎯 Задание

### Задача 1: Object Mapper

Реализуй простой маппер объектов через reflection:
- Копирование полей по имени
- Поддержка вложенных объектов

### Задача 2: Dependency Injector

Реализуй простой DI контейнер:
- Аннотация @Inject
- Автоматическое внедрение зависимостей

### Задача 3: toString() генератор

Автоматическая генерация toString() через reflection.

## ✅ Чек-лист

- [ ] Понимаешь разницу getDeclared* vs get*
- [ ] Знаешь про setAccessible и модули JPMS
- [ ] Понимаешь влияние на производительность
- [ ] Умеешь работать с generics через reflection

